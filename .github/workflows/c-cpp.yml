name: C/C++ CI

on:
  push:
    branches: [ "devin/1774626930-android-studio-mobile-ide" ]
  pull_request:
    branches: [ "devin/1774626930-android-studio-mobile-ide" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
